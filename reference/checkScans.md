# Check if any samples are missing spectra

Display warning if they do. Currently warns user if a sample is detected
that has less than 50% of the mean of spectra in all samples.

## Usage

``` r
checkScans(spectra)
```

## Arguments

- spectra:

  `Spectra` object

## Author

Pablo Vangeenderhuysen
